import { Content } from '../../hooks/usePDFContent'
import { Header } from '../../hooks/usePDFContent'

export interface TableProps {
  content: null | Content
}

export interface TableHeaderProps {
  content: null | Header
}
